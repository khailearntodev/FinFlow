services:
  postgres:
    image: postgres:latest
    container_name: finflow_postgres
    environment:
      - POSTGRES_DB=mydatabase
      - POSTGRES_USER=myuser
      - POSTGRES_PASSWORD=secret
    ports:
      - "5432:5432"
    volumes:
      - pgdata:/var/lib/postgresql/data

  # 2. Backend Service (Spring Boot)
  backend:
    build:
      context: ./finflow-backend
      dockerfile: Dockerfile
    container_name: finflow_backend
    ports:
      - "8080:8080"
    environment:
      - SPRING_DATASOURCE_URL=jdbc:postgresql://postgres:5432/mydatabase
      - SPRING_DATASOURCE_USERNAME=myuser
      - SPRING_DATASOURCE_PASSWORD=secret
      - SPRING_JPA_HIBERNATE_DDL_AUTO=update
    depends_on:
      - postgres

  # 3. Frontend Service (Next.js)
#  frontend:
#    build:
#      context: ./finflow-frontend
#      dockerfile: Dockerfile
#    container_name: finflow_frontend
#    ports:
#      - "3000:3000"
#    environment:
#      - NEXT_PUBLIC_API_URL=http://localhost:8080 # Trỏ API gọi về Backend
#    depends_on:
#      - backend

volumes:
  pgdata: